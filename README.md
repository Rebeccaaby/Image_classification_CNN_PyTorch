# Image_classification_CNN_PyTorch
This project builds an image classification model on the OCTMNIST dataset using PyTorch. Custom CNN - trained through experiments with different hyperparameters to improve performance. The model was evaluated using accuracy, confusion matrices, and ROC curves.
